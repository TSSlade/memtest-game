"""Repository and operator tooling that ships with the package.

`generate_alerts` travels with the assets it produces so that an installed copy
can be verified against its own generator, rather than the reproducibility claim
in MODIFICATIONS.md holding only for a source checkout.
"""
