{
  "_profile": "smoke-test",
  "_comment": [
    "This is a SMOKE-TEST protocol, not a research one. Its job is to run end",
    "to end in seconds so you can confirm audio, display and output paths work",
    "before a participant is in the chair. Two trials and a five-second",
    "baseline are not a study.",
    "To run a real protocol: copy example-config.json, adapt it to your design,",
    "and pass it with `memtest --config my-protocol.json`. Do not edit this",
    "file -- it lives inside the installed package.",
    "Keys beginning with an underscore are metadata and are ignored by the",
    "configuration loaders."
  ],
  "session": {
    "num_trials": 2,
    "trials_per_effort": 1,
    "wait_baseline": 5,
    "wait_breaks": 2,
    "wait_endline": 5
  },
  "game": {
    "num_hexagons": 36,
    "score_threshold": 0.7,
    "enable_audio_alerts": true,
    "volume": 0.5,
    "audio_files": {
      "start_baseline": "assets/alerts/warm/start_baseline.wav",
      "start_game": "assets/alerts/warm/start_game.wav",
      "start_break": "assets/alerts/warm/start_break.wav",
      "end_break": "assets/alerts/warm/end_break.wav",
      "start_endline": "assets/alerts/warm/start_endline.wav",
      "end_game": "assets/alerts/warm/end_game.wav",
      "task_complete": "assets/alerts/warm/task_complete.wav"
    }
  }
}
